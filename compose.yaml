services:
  door-management:
    build: .
    user: "1000:1000"
    ports:
      - "4173:4173"
    volumes:
      - ./data:/data
    environment:
      FETCH_ACTUAL_USERS: true
    restart: unless-stopped